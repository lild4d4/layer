DESIGN_NAME: auth
VERILOG_FILES:
- src/auth_init.sv
- src/auth_generate_challenge.sv
- src/auth_verify_id.sv
- secworks-aes/src/rtl/aes.v

USE_SLANG: true

# 30% because the design is too small.
FP_CORE_UTIL: 30

FP_ASPECT_RATIO: 1.0
PL_TARGET_DENSITY: 0.55
